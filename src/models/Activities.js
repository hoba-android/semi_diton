class Activities {
  constructor(uid, water, running, football, walking) {
    this.uid = uid;

    this.water = water;
    this.running = running;
    this.football = football;
    this.walking = walking;
  }
}
export default Activities;
