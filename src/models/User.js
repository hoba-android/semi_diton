class User {
  constructor(uid, age, pack, name, city, address, totalmeals, orderedMeals) {
    this.uid = uid;
    this.age = age;
    this.pack = pack;
    this.name = name;
    this.city = city;
    this.address = address;
    this.totalmeals = totalmeals;
    this.orderedMeals = orderedMeals;
  }
}
export default User;
